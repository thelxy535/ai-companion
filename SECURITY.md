# 安全说明

## 报告安全问题

这是一个个人开发项目。请不要在公开 Issue 中发布 API Key、设备令牌、数据库、聊天记录或完整日志。发现安全问题时，请通过仓库维护者的私下联系方式报告，并提供最小化的复现信息。

## 本地数据边界

- 聊天记录、记忆和角色资料默认保存在 Android 设备本地。
- API Key 由用户在应用内配置，不应写入源码、资源文件或提交历史。
- 视觉网关使用设备令牌和请求限制，不应暴露为公共通用代理。
- 生产环境部署前应替换示例凭据、限制监听地址并检查系统防火墙。

## 提交前检查

```powershell
rg -n --hidden -g '!**\\build\\**' -g '!**\\.gradle\\**' "sk-[A-Za-z0-9]{20,}" .
```

命令没有输出才表示当前源码树未发现常见格式的 API Key；这不能替代正式的密钥扫描工具。
