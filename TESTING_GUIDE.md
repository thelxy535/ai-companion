# 测试指南

## 本地验证

在项目根目录执行：

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat lintDebug
.\gradlew.bat assembleDebug
```

Debug APK 输出到 `app/build/outputs/apk/debug/app-debug.apk`。安装前确认使用的是刚刚构建的 APK，不要把 APK、截图、日志或数据库加入 Git。

## 模拟器回归重点

### 聊天与历史

- 连续发送超过 50 条消息，确认界面保持流畅且新消息可见。
- 让角色主动发消息后再回复，确认回复能关联主动消息。
- 在历史消息很多时提及旧主题，确认模型上下文仍可召回相关内容。
- 发送失败后重试，确认不会重复插入用户消息或空白助手消息。

### 角色与场景

- 分别使用外向、慢热和高敏感角色，确认语气、长度和主动性有差异。
- 让角色处于明确穿着和地点，短时间连续对话，确认不会无理由换场景。
- 明确说“换上外套并出门”，确认场景只在有转场依据时更新。
- 输出动作描写，确认动作显示在对白下方且不会混入正文。

### 记忆与数据

- 查看记忆树、时间线和原始记录，确认中文正常显示。
- 创建、编辑、导入、导出和删除自定义角色。
- 删除角色前确认生成记忆胶囊，恢复后确认数据范围正确。
- 修改数据库结构时运行 Room migration 测试。

## 真机专项

真机只用于最终确认系统相关行为：通知权限、锁屏通知、后台回复通知、返回手势、状态栏和桌面图标。不得清空应用数据，不得操作与测试无关的应用。

## 测试环境

- Android Studio Hedgehog 或更高版本
- JDK 17
- Android SDK 34
- 已配置的 OpenAI 兼容 API，或本地 mock 服务
