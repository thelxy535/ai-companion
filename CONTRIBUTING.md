# 参与开发

弦曜 SYLORA 当前是个人持续开发项目。提交修改前，请先确认改动属于真实功能、稳定性、可访问性或文档改进，并避免提交本地运行产物。

## 开发流程

1. 从最新分支创建功能分支。
2. 保持一个提交只解决一类问题，提交信息使用简短的英文动词描述。
3. 修改数据库时，同时更新 migration 和 `app/schemas/`。
4. 修改聊天、记忆、角色行为或通知时，补充对应的单元测试。
5. 提交前运行：

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat lintDebug
.\gradlew.bat assembleDebug
```

## 不要提交

- API Key、设备令牌、数据库和个人聊天记录
- APK、截图、UI dump、日志和模拟器数据
- `local.properties`、签名文件和本地服务配置
- 临时脚本、编辑器缓存和源码备份文件

## 代码约定

- 优先使用现有的 Compose、Room、Flow 和 Hilt 模式。
- UI 展示限制不能成为模型记忆限制；需要限制时，应在明确的上下文预算层处理。
- 角色行为应由角色配置和内在状态共同决定，避免把所有角色写成同一种固定规则。
- 任何用户可见的行为变化都要考虑加载、失败、空数据和后台恢复状态。
